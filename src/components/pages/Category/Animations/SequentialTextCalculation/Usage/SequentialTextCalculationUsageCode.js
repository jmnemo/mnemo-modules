const SEQUENTIAL_TEXT_CALCULATION_USAGE_CODE = (
`<SequentialTextCalculation
  strArr={Array<string>}
  step={number}
  delay={number}
  minCharCode={number}
  maxCharCode={number}
  isAnimationActive={boolean}
  animationResetTrigger={number|boolean}
  decodingWhenAnimationIsPaused={boolean}
  animationEndCallback={function}
/>`);

export default SEQUENTIAL_TEXT_CALCULATION_USAGE_CODE;